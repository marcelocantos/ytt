# Build artefacts.
/ytt
/dist/

# Go
*.test

# Editor and OS noise.
.vscode/
.idea/
.DS_Store

# Local channel config stays out of the repo: it is personal, and resolving
# config from the install prefix is what caused a fifteen-day silent outage.
scripts/playlist-ingest/channels.yaml
